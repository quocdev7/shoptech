@model IEnumerable<shopdodadung.Models.Mcategogy>
    @{  var menuitem = ViewBag.menuitem;}
    @*<li><a href="~/@menuitem.Slug">@menuitem.Name</a>
        @if (Model.Count() != 0)
        {
            <ul>
                @foreach (var cat in Model)
                {
                    <li><a href="~/@cat.Slug">@cat.Name</a></li>
                }
            </ul>
        }
    </li>*@
